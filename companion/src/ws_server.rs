// WebSocket server stub — communicates with the DAW browser frontend.
// Will be implemented in a separate work unit.

/// Placeholder for the WebSocket server that bridges the DAW UI and the companion.
pub struct WsServer;

impl WsServer {
    pub fn new() -> Self {
        Self
    }
}
