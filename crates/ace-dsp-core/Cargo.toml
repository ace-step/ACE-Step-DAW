[package]
name = "ace-dsp-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure DSP algorithms for ACE-Step-DAW (no_std compatible)"

[features]
default = ["std"]
std = []

[dependencies]

[dev-dependencies]
