// VST3 plugin host stub — loads and manages plugin instances.
// Will be implemented in a separate work unit.

/// Placeholder for the VST3 plugin host.
pub struct PluginHost;

impl PluginHost {
    pub fn new() -> Self {
        Self
    }
}
